# `@jfdevelops/multi-step-form-core` docs

- [Migrating from alpha](./migration.mdx) — moving step-level `overrides` to `.withOverrides(...)`.
- [Instances, storage & field metadata](./instances-and-storage.mdx) — `defineMultiStepForm`,
  named instances, per-instance storage, shared `createHelperFn`, `withOverrides`, field
  `isRequired`/`placeholder`/`errorMessage`, step `isComplete`, and a type cookbook.
