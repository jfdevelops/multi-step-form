---
'@jfdevelops/react-multi-step-form': patch
---

Allow reusable factory field components to bind to any created multi-step form schema, including schemas finalized with `withForm()` and `withContext()`.
