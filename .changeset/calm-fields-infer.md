---
'@jfdevelops/react-multi-step-form': patch
---

Preserve the full step field union when `createComponent.forField` omits `fields`, while narrowing only when an explicit `fields` selector is provided.
