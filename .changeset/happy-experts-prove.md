---
"@jfdevelops/react-multi-step-form": patch
---

Adds new `selectorFn` prop to the `Field` component. Providing a value for the `selectorFn` will internally wrap the `<Field />`'s children with a `<Selector />`.
